{
  "name": "release-fn",
  "version": "1.0.0",
  "description": "Timer-triggered Azure Function that promotes the next daily puzzle in the catalog blob. Replaces the GitHub Actions release workflow + Logic App pair — see FEATURE.md Feature P Phase 2.",
  "private": true,
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "node ../../scripts/build-release-fn.mjs",
    "package": "npm run build && cd dist && powershell -NoProfile -Command \"Compress-Archive -Path * -DestinationPath ../release-fn.zip -Force\""
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@azure/identity": "^4.4.1",
    "@azure/storage-blob": "^12.24.0"
  }
}
