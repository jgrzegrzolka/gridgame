# gridgame

## Run locally

Requires [Node.js](https://nodejs.org).

```powershell
.\runlocally.ps1
```

Then open <http://localhost:3000/>. Ctrl+C to stop.

Opening the HTML files directly via `file://` won't work — `fetch()` needs HTTP.
